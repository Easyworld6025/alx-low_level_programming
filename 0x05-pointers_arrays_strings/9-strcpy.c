#include "main.h"

/**
 * _strcpy -copy the string
 * @dest: Destination value
 * @src: Source value
 * Return: pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
int l;
for (1 = 0; src[i] != '\0'; i++)
{
dest[i] = src[i];
}
dest[i++] = '\0';
return (dest);
}
