#include "main.h"
/**
 * _abs - it must computes the absolute value of an integer
 * @n: integer to be check
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
