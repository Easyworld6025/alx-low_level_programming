#include "main.h"
/**
 * _islower - program checks for lowercase chracter
 * @c: the character to check
 * Return: 1 if c is lowercase, 0 if its not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
